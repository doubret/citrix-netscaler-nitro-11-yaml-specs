scope:                                    compression
fields:
  name:                                   string
  rule:                                   string
  resaction:                              cmpaction.name
key:
  - name
update:
  - rule
  - resaction
