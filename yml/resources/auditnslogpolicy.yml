scope:                                    audit
fields:
  name:                                   string
  rule:                                   string
  action:                                 auditnslogaction.name
key:
  - name
update:
  - rule
  - action
