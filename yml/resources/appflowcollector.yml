scope:                                    appflow
key:
  name:                                   name
fields:
  ipaddress:                              ip
  port:                                   int
  netprofile:                             netprofile.name
update:
  - ipaddress
  - port
  - netprofile
