scope:                                    traffic-management
fields:
  name:                                   string
  rule:                                   string
  action:                                 tmsessionaction.name
key:
  - name
update:
  - rule
  - action
