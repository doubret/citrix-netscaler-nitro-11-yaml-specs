scope:                                    spillover
fields:
  name:                                   string
  action:                                 (SPILLOVER)
key:
  - name
operations:
  - get
  - list
  - rename
  - count
