scope:                                    domain-name-service
fields:
  actionname:                             string
  prefix:                                 string
  mappedrule:                             string
  excluderule:                            string
key:
  - name
update:
  - prefix
  - mappedrule
  - excluderule
