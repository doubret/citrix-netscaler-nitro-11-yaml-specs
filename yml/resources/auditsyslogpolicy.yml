scope:                                    audit
fields:
  name:                                   string
  rule:                                   string
  action:                                 auditsyslogaction.name
key:
  - name
update:
  - rule
  - action
operations:
  - get
  - update
  - unset
  - list
  - rename
  - count
