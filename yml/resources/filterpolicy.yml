scope:                                    filter
fields:
  name:                                   string
  rule:                                   string
  reqaction:                              filteraction.name
  resaction:                              filteraction.name
key:
  - name
update:
  - rule
  - reqaction
  - resaction
operations:
  - get
  - update
  - unset
  - list
  - rename
  - count
